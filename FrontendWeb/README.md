;
Voici mon cahier des charges  
- je veux que tu annalyse bien le cahier des charges et que tu l'adapte à ce projet avec une bonne structure et des composants reuitilisable dans tout le projet 
- et de bien preparer le projet a fin qu'il puisse recevoir le back end sans problème  





je veux m'maintenant continuiez sur le module administrateur,       et je veux que tu m'aide a complété ce fichier en fonction de ces composant que j'ai envoi pour que tout fonctionne bien et   avec la possibilité de faire(valider avec confirmation, rejeté avec confirmation, voir ),exporter au choix(pdf, cvs, doc),  paginé, rechercher, filtrée, l'affichage des données et les actions dans le tableau, ...  tu fait bien et  que tout soit dynamique avec des composants réutilisable pour éviter la duplication de donnée et tu fait tout comme un export en développement Web front end 




j'ai veux que tu m'aide a continuiez le projet qu'on a commence précédemment et voici mon cahier des charges





## SUGGESTION:

#### INTERFACE ADMI

# Dashboard:   
--    Performance du jour  98.5% = ?
--    Activité récente = Les 5 trajets recent
--    Voir tous = redirige a trajets
--    Trajets aujourd'hui = tous les trajets

# Profil:
--  pourvoir ajouter un autre admin
--  modifier ces information avec photo de profle 
--  Bio = Sup
--  Jours actifs = Sup
--  Safari sur iPhone = Sup
--  Voir tout le journal → = redirige sur la table historique
--  Sur clic sur modifier le proflile degrige les champs pour un btn pour valide les modification
--  Changer le mot de passe = ajoute sur les champs un oeil pour voir le mode pass
--  Gestion des appareils = alert déconnexion
--  Emails marketing = sup
--  Préference = mode, ...

# Parametre:
--  Fuseau horaire = logo
--  Nouvelle demande de trajet = sup
--  Statut du trajet = sup
--  Alertes administratives = sup
--  Configuration,Coût/SMS (GNF)= sup
--  Securiter = sup
--  Api = grise

# Notification:

--  Notifications = sur clic sur une on affiche la liste des ces types notifs
--  Voir toutes les notifications = sur clic affichage de toutes les notifs

#  User:
   Su clic affiche (passager/ chauffeur)

# Passager:
--  Taux d'engagement = sup
--  logo sur les cards = ajoustement 
--  Suspendre = sup
--  badge Conducteur = sup
--  Liste des utilisateurs = Liste des passagers
--  Tris pas Role = sup
--  Action = sup(supprimer, action)
--  Action Détails = sup(modifier, supprimer, premium, document, adress, contacter(numero tel) les info perso descend)

# Chauffeur:
--  Action = activer, désactiver, suspendre
--  Filtre par status = en ligne, hors ligne, activer, desactiver, suspendue
--  Status verification = sup
--  Detail adress , Ville = sup

# Document:
-- Affichier l'ensemble des document par chauffeur sur chaque ligne de document on peux faire (valider, non valider, voir)
-- Supprimer = sup
-- Stockage utilisé = sup
-- filtre Rôle du propriétaire, Type de document = sup
-- Répartition des documents = sup

# Sidbar: ?

# Validation: 
--   Voir plus de demandes = sup


# Trajet:
--  Dans detail Carburant, anne, Évaluation(chauffeur), = sup
--  Dans action Archiver, supprimer = sup
--  Frais de plateforme = gain de taka taka

# Paiement
--  Dans card , Taux de réussite = sup
--   Dans card Transactions = total des paiement
--  Répartition par méthode Virements, Wave = sup
--  Sur la graphe Taux de conversion = sup
--  filtre Status = rambourser
--  filtre tous = sup
--  filtre par une seule date
-- bouton generer = sup
--  Dans action Archiver, supprimer = sup
--  Indicateurs clés, Tendances et insights =sup

# Commmission:
-- Ce Mois = toute les commission
-- Taux moyen = sup
-- Filtre virement, livraison = sup
-- Dans modifier Nombre de trajets,Taux de commission = griser 
-- Historique des notes, supprimer = sup
-- Taux moyen, Croissance mensuelle = sup

# Litiges:
-- Ouverts = Rejeter
-- Temps moyen = total des litige
-- Dans filtre Status Ouvert = sup
-- Filtre priorite = sup
-- Detail Abidjan-Plateau, moyene, Montant, Agent assigné, supprimer = sup 
-- Trajet concerné = Depart, destination
-- Historique des commentaires = sup
-- Performance de résolution = sup
# Rapport:
-- Statut En attente, echoue = sup
-- Dans les card En attente,Taille totale = sup
-- Filtre geographique = trajets
-- Activité de génération = sup
-- Detail Métriques, supprimer, Version: = sup
-- Regenerer = telecharger
-- Indicateurs clés
-- Taux de génération Recommandations et insights  = sup








### RESTE A ASSAMBLE
-- Layout
-- ui
-- thems